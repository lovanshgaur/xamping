/** Single Local Storage key. Never split persistent data across keys. */
export const STORAGE_KEY = "bootxamp";

/** Envelope version — bump when the wrapper changes. */
export const ENVELOPE_VERSION = 1;

/** Data schema version — bump and add a migration when BootXampData changes. */
export const SCHEMA_VERSION = 1;

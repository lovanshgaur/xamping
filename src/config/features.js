/** Feature flags. Kept centralized so behavior toggles never leak into components. */
export const FEATURES = Object.freeze({
  animations: true,
  notifications: true,
  achievements: true,
  analyticsPngExport: true,
  autoCarryForward: true,
});
